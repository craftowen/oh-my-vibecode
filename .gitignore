# deps / build
node_modules
build
dist
.react-router

# internal planning — kept locally, not published
PLAN.md

# secrets — never commit
.dev.vars
.dev.vars.*
.env
.env.*
!.dev.vars.example
!.env.example

# cloudflare local state (local D1 sqlite, KV, R2 live here)
.wrangler

# test / coverage / logs
coverage
*.log
npm-debug.log*

# editor / os
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
